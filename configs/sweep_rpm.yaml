# RPM sweep at fixed water depth (README section 20, Study A)
base_config: configs/baseline.yaml

water_height_m: 0.050

rpm_coarse:
  - 300
  - 500
  - 700
  - 900
  - 1100
  - 1300
  - 1500

bisection:
  target_resolution_rpm: 5
  max_iterations: 8
  persistence_rotations: 5
